[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "verde"
version = "1.0.0"
description = "Minimal Roblox Studio place export/import utility — lightweight alternative to Rojo (Python + Luau + Studio plugin)"
readme = "README.md"
license = { text = "Unlicense" }
authors = [{ name = "misterdustinface" }]
requires-python = ">=3.9.6"
keywords = ["roblox", "rbxlx", "version-control", "export", "import", "luau", "plugin"]
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: The Unlicense (Unlicense)",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Build Tools",
    "Topic :: Games/Entertainment",
]
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[project.scripts]
# Offline place file tools
verde-export = "extract:main"
verde-import = "build:main"
# Offline folder ↔ .rbxlx (mtime-win). Future: real git-merge-style conflict resolution.
verde-merge = "features.sync:main"
# Live folder ↔ open Studio (HTTP bridge + watcher)
verde-sync = "features.bridge:main"
# Helpers
verde-search = "features.search:main"
verde-replace = "features.set_replace:main"
verde-set = "features.set_replace:main"
verde-tags = "features.tags:main"
verde-test-roundtrip = "test_roundtrip:main"

[tool.setuptools]
package-dir = { "" = "python" }
packages = ["features"]
py-modules = [
    "extract",
    "build",
    "interesting",
    "test_roundtrip",
    "attributes",
]

[tool.pytest.ini_options]
testpaths = ["python/tests"]
pythonpath = ["python"]
